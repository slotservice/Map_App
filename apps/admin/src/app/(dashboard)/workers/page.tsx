export default function WorkersPage() {
  return (
    <section>
      <h1 className="text-2xl font-semibold">Workers</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        TODO(week-1): worker CRUD table powered by GET /users?role=worker.
      </p>
    </section>
  );
}
